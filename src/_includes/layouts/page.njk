---
layout: layouts/base.njk
---

{{ content | safe }}
{% if navi.reverse %}
  {% set listing = collections.all | navi(navi.key) | reverse %}
{% else %}
  {% set listing = collections.all | navi(navi.key) %}
{% endif %}

{%- for item in listing  %}
{% set kolor =  [1, 2, 3 ] | removeVal(kolor) | random %}

{% if item.altTitle %}
	<h2 class="bloop-base bloop{{kolor}}"><a href="{{ item.url }}" >{{ item.altTitle }}</a></h2>
{% else %}
	<h2 class="bloop-base bloop{{kolor}}"><a href="{{ item.url }}">{{ item.title | splitWords | capitalize }}</a></h2>
{% endif %}

  {% if item.cover and not hideCovers %}
  <figure>
    <a href="{{ item.url }}"><img src="{{ item.cover }}" /></a>
    <figcaption><em>{{ item.excerpt}}</em></figcaption>
  </figure>
   {% else %}
    <em>{{ item.excerpt }}</em>
   {% endif %}

{%- endfor%}